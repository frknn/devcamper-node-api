{
  "name": "devcamper_api",
  "version": "1.0.0",
  "description": "Devcamper app backend API",
  "main": "server.js",
  "scripts": {
    "start": "SET NODE_ENV=production& node server",
    "dev": "nodemon server",
    "seed": "node seeder -b -i && node seeder -c -i",
    "destroy": "node seeder -c -d && node seeder -b -d",
    "dns": "node seeder -c -d && node seeder -b -d && node seeder -b -i && node seeder -c -i"
  },
  "author": "Furkan Setbasi",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-fileupload": "^1.1.6-alpha.6",
    "mongoose": "^5.7.5",
    "morgan": "^1.9.1",
    "node-geocoder": "^3.24.0",
    "slugify": "^1.3.5"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
